# DataTune

GitHub Pages site for the discontinued DataTune data-cleansing product (`index.html`, `params.json`).

## Unit tests

`Tests/datatune.test.js` checks the Datatune page title/heading, download zip control, and `params.json` name/tagline. Run `node Tests/datatune.test.js`.
